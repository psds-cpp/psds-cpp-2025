#include <cstddef>
#include <stdexcept>


size_t CharChanger(char array[], size_t size, char delimiter = ' ') {
    size_t convertedSymbol = 0; currentSybmol

    for (size_t i = 0; i < size; ++i) {    
        if (!isalpha(array[])) {
            
        }
    }


    throw std::runtime_error{"Not implemented"};
}
